server:
  port: 9002

spring:
  zipkin:
    base-url: http://localhost:9411/zipkin/
  application:
    name: contact-service

eureka:
  instance:
    hostname: localhost

    management:
      tracing:
      sampling:
        probability: 1.0